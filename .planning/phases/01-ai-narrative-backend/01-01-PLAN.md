---
phase: 01-ai-narrative-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/test_ai_interpret.py
autonomous: true
requirements:
  - AI-05
must_haves:
  truths:
    - "anthropic package is importable after pip install"
    - "Test file exists with failing stubs for all AI-04 behaviors (score, auth error, rate limit, invalid context_type)"
    - "Mock AsyncAnthropic fixture is defined so Wave 1 tests can run without a real API key"
  artifacts:
    - path: "pyproject.toml"
      provides: "anthropic>=0.84 in project dependencies"
      contains: "anthropic>=0.84"
    - path: "tests/test_ai_interpret.py"
      provides: "Full test scaffold for AI-01 through AI-05"
      exports:
        - test_anthropic_importable
        - test_interpret_score_returns_narrative
        - test_interpret_auth_error_returns_503
        - test_interpret_rate_limit_returns_429
        - test_interpret_invalid_context_type_returns_422
        - test_score_narrative_prompt_contains_pd
        - test_forecast_narrative_prompt_contains_threshold
        - test_monitoring_narrative_prompt_reflects_drift
  key_links:
    - from: "tests/test_ai_interpret.py"
      to: "service/api.py"
      via: "FastAPI TestClient importing api.app"
      pattern: "from service.api import app"
---

<objective>
Install the anthropic SDK dependency and create the test scaffold for all AI narrative tests.

Purpose: Establish the Nyquist-compliant test foundation before any implementation. The test file defines expected behaviors; tests will be RED until Plan 02 implements the endpoint.
Output: Updated pyproject.toml with anthropic>=0.84, tests/test_ai_interpret.py with 8 stubs and a mock AsyncAnthropic fixture.
</objective>

<execution_context>
@/Users/chivonpowers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chivonpowers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ai-narrative-backend/01-RESEARCH.md
@.planning/phases/01-ai-narrative-backend/01-VALIDATION.md

<interfaces>
<!-- Existing test pattern from tests/test_api_contract.py -->
```python
from fastapi.testclient import TestClient
import service.api as api

client = TestClient(api.app, raise_server_exceptions=False)
resp = client.post("/some/endpoint", json={...})
assert resp.status_code == 200
```

<!-- schemas.py BaseModel pattern (existing) -->
```python
from pydantic import BaseModel, Field
class SomeRequest(BaseModel):
    field: str = Field(description="...")
```

<!-- anthropic error hierarchy (from RESEARCH.md) -->
# anthropic.AuthenticationError → HTTP 401 from Anthropic
# anthropic.RateLimitError → HTTP 429 from Anthropic
# anthropic.APIStatusError → Other 4xx/5xx from Anthropic
# anthropic.APIConnectionError → Network failure
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add anthropic dependency and create test scaffold</name>
  <files>pyproject.toml, tests/test_ai_interpret.py</files>
  <behavior>
    - test_anthropic_importable: `import anthropic` succeeds without ImportError
    - test_interpret_score_returns_narrative: POST /ai/interpret with context_type="score" and valid score data returns 200 with {"narrative": str}
    - test_interpret_auth_error_returns_503: When AsyncAnthropic raises AuthenticationError, endpoint returns 503
    - test_interpret_rate_limit_returns_429: When AsyncAnthropic raises RateLimitError, endpoint returns 429
    - test_interpret_invalid_context_type_returns_422: POST /ai/interpret with context_type="invalid" returns 422
    - test_score_narrative_prompt_contains_pd: The prompt sent to Claude for a score context includes the pd value as a percentage
    - test_forecast_narrative_prompt_contains_threshold: The prompt sent to Claude for a forecast context includes threshold exceedance info
    - test_monitoring_narrative_prompt_reflects_drift: The prompt sent to Claude for a monitoring context includes drift severity info
  </behavior>
  <action>
    Step 1 — Add dependency to pyproject.toml:
    In the [project] dependencies list, add `"anthropic>=0.84",` after the httpx line. Then run `pip install "anthropic>=0.84"` to install it.

    Step 2 — Create tests/test_ai_interpret.py:
    Write the full test file with these requirements:

    IMPORTS: Use `from unittest.mock import AsyncMock, MagicMock, patch` and `from fastapi.testclient import TestClient` and `import service.api as api`.

    MOCK FIXTURE: Define a pytest fixture `mock_anthropic_client` that patches `service.api._get_anthropic_client`. The mock must return an object where `messages.create` is an `AsyncMock` that returns a mock message. The mock message's `content[0].text` must return the string `"Test narrative."` and `content[0]` must have a `text` attribute (not just dict access). Use `MagicMock` for the content object with `spec` or explicit attribute assignment.

    IMPORTANT: The endpoint does not exist yet — tests will FAIL (RED state) until Plan 02 creates it. Write the tests as if the endpoint exists at its final spec:
    - POST /ai/interpret accepts `{"context_type": "score"|"forecast"|"monitoring", "data": {...}}`
    - Returns `{"narrative": "..."}` on success
    - Returns 503 when AuthenticationError raised
    - Returns 429 when RateLimitError raised
    - Returns 422 for invalid context_type

    For error tests: patch `service.api._get_anthropic_client` to return a mock whose `messages.create` side_effect is the relevant anthropic exception class instantiated with a dummy message. Use `anthropic.AuthenticationError`, `anthropic.RateLimitError` — check anthropic SDK source for correct constructor args (they may require `response` kwarg; use `MagicMock()` as the response if needed).

    For prompt content tests (test_score_narrative_prompt_contains_pd etc.): capture the `messages=[{"role": "user", "content": PROMPT}]` kwarg passed to `messages.create` and assert the prompt string contains the expected numeric values (e.g., `"34%"` or the pd value formatted as percentage).

    Sample score data for tests:
    ```python
    SCORE_DATA = {"pd": 0.34, "decision": "default", "top_factors": [{"name": "original_ltv", "value": 0.12}]}
    FORECAST_DATA = {"forecast": [{"ds": "2026-01-01", "yhat": 0.18}, {"ds": "2026-02-01", "yhat": 0.25}], "threshold": 0.20}
    MONITORING_DATA = {"drift_features": {"feature1": {"psi": 0.25}}, "score_drift": {"alert": True}, "perf_drift": {"auc": 0.74}}
    ```

    Run tests after writing: `pytest tests/test_ai_interpret.py -x -v` — expect FAILURES (RED is correct at this stage).
  </action>
  <verify>
    <automated>python -c "import anthropic; print('anthropic importable, version:', anthropic.__version__)" && pytest tests/test_ai_interpret.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>pyproject.toml contains "anthropic>=0.84". tests/test_ai_interpret.py exists with 8 test functions and a mock fixture. anthropic is importable. Tests run (expected to FAIL until Plan 02 — FAILED status is correct here, NOT an error in Plan 01).</done>
</task>

</tasks>

<verification>
- `grep "anthropic" /Users/chivonpowers/predictive-modeling-ai/pyproject.toml` returns the dependency line
- `python -c "import anthropic"` exits 0
- `pytest tests/test_ai_interpret.py --collect-only` shows 8 test items
</verification>

<success_criteria>
anthropic>=0.84 is in pyproject.toml and installed. tests/test_ai_interpret.py has 8 test functions covering AI-01 through AI-05 behaviors. All tests are collected by pytest (they may fail RED — that is expected and correct).
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-narrative-backend/01-01-SUMMARY.md`
</output>
