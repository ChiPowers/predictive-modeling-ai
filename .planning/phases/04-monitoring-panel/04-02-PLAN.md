---
phase: 04-monitoring-panel
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified: []
autonomous: false
requirements:
  - MON-01
  - MON-02
  - MON-03
  - MON-04

must_haves:
  truths:
    - "Model Health section is visible with colored drift badges instead of raw JSON"
    - "Each feature badge is green (PSI <0.1), yellow (0.1-0.2), or red (>0.2)"
    - "AUC row shows 'Not yet available' when perf_drift is null (demo state)"
    - "AI narrative paragraph appears below the drift panel after data loads"
    - "Refreshing monitoring reloads all panel content without page reload"
  artifacts:
    - path: "service/static/index.html"
      provides: "Confirmed: no monitoringView pre, driftIndicators and modelAucRow divs present"
    - path: "service/static/app.js"
      provides: "Confirmed: renderMonitoringPanel() renders panel content on page load"
  key_links:
    - from: "Refresh Monitoring button"
      to: "loadMonitoring() -> renderMonitoringPanel()"
      via: "click event listener in initMonitoringSection()"
      pattern: "refreshMonitoringBtn"
---

<objective>
Human visual verification of the Phase 4 monitoring panel. Confirms all four MON requirements are met from a visitor's perspective — drift badges render correctly, AUC row shows the graceful fallback, narrative loads asynchronously, and the Refresh button works.

Purpose: Frontend rendering cannot be verified by pytest (no JS test runner). This checkpoint is the quality gate for Phase 4.
Output: Human approval or issues list for gap closure.
</objective>

<execution_context>
@/Users/chivonpowers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chivonpowers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-monitoring-panel/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite</name>
  <files></files>
  <action>
Run the full test suite to confirm no regressions from Phase 4 changes:

```bash
cd /Users/chivonpowers/predictive-modeling-ai && pytest tests/ -x -q
```

If tests pass, output the human verification checklist so the user knows what to check in the browser.
  </action>
  <verify>
    <automated>cd /Users/chivonpowers/predictive-modeling-ai && pytest tests/ -x -q</automated>
  </verify>
  <done>Full test suite passes green with no failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification — Model Health panel</name>
  <files></files>
  <action>
Present the human verification checklist to the user. The app must be running at http://localhost:8000 (start with `uvicorn service.api:app --reload` if not running). No automated action — this task pauses for human confirmation.
  </action>
  <what-built>
Phase 4 complete: Model Health panel with per-feature drift badges, AUC row, and AI narrative replacing the raw JSON monitoringView pre element.
  </what-built>
  <how-to-verify>
Open the app at http://localhost:8000 (start with `uvicorn service.api:app --reload` if not running).

Check the Monitoring Summary section (scroll to the bottom):

1. **MON-01 - Section loads from API:** The Monitoring Summary section should NOT show a raw JSON block or `<pre>` element. It should show structured content.

2. **MON-02 - Drift badges:** Feature drift badges should appear — one badge per feature (credit_score, orig_ltv, orig_dti, orig_upb, orig_interest_rate, orig_cltv). Each badge should be colored:
   - Green = PSI < 0.1
   - Yellow = PSI 0.1-0.2
   - Red = PSI > 0.2
   With the demo data all PSI values should be 0.0, so all badges should be green.
   A legend line should appear: "Feature drift (PSI): green <0.1 · yellow 0.1-0.2 · red >0.2"

3. **MON-03 - AUC row:** Below the drift badges, a row should show: "AUC: Not yet available (labels pending)" — this is correct for demo mode where perf_drift is null.

4. **MON-04 - AI narrative:** If ANTHROPIC_API_KEY is set in your environment, a narrative paragraph should appear below the panel after a few seconds. If the key is not set, the narrative section stays hidden (this is acceptable — the Phase 1 implementation degrades gracefully).

5. **Refresh button:** Click "Refresh Monitoring" — the panel should reload and re-render without a full page refresh. No console errors.

6. **No raw JSON visible:** Confirm there is no `<pre>` element or raw JSON object displayed in the Monitoring Summary section.

Open browser DevTools console and confirm no JavaScript errors during page load or monitoring refresh.
  </how-to-verify>
  <verify>
    <automated>MISSING — visual rendering requires human verification (no JS test runner in project)</automated>
  </verify>
  <done>Human confirms all 6 visual checks pass and types "approved".</done>
  <resume-signal>Type "approved" if all 6 checks pass, or describe which checks failed with observed behavior.</resume-signal>
</task>

</tasks>

<verification>
All four MON requirements verified by human observation:
- MON-01: Monitoring section loads from GET /monitoring/summary with structured output
- MON-02: Per-feature drift badges with correct green/yellow/red PSI coloring
- MON-03: AUC value and trend indicator (or graceful null fallback)
- MON-04: AI narrative appears below panel (when API key available)
</verification>

<success_criteria>
- pytest tests/ -x -q passes (no regressions)
- Human confirms all 6 visual checks pass
- No JavaScript console errors during monitoring load or refresh
- Phase 4 milestone complete — all MON-01 through MON-04 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-monitoring-panel/04-02-SUMMARY.md`
</output>
