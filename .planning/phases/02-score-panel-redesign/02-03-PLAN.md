---
phase: 02-score-panel-redesign
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified: []
autonomous: false
requirements: [VIZ-01, VIZ-02, VIZ-03, VIZ-04, SCEN-01, SCEN-02, SCEN-03]

must_haves:
  truths:
    - "Visitor sees a color-coded arc gauge and risk tier badge after scoring — no raw JSON visible"
    - "Factor bars appear below the gauge showing which features drove the score"
    - "Scenario buttons populate the form with realistic preset values; textarea remains editable"
    - "All Phase 1 narratives still appear (score narrative wired through to gauge panel)"
  artifacts:
    - path: "service/static/index.html"
      provides: "Score panel with gauge, badge, factor bars — no scoreView pre"
    - path: "service/static/app.js"
      provides: "renderScorePanel wired and all scenario buttons functional"
  key_links:
    - from: "user clicks 'Score Loan'"
      to: "renderScorePanel(payload)"
      via: "initScoreForm() submit handler"
      pattern: "renderScorePanel"
    - from: "user clicks 'Prime Borrower'"
      to: "features textarea"
      via: "scenario click handler sets form.features.value"
      pattern: "SCENARIOS"
---

<objective>
Human verification that the visual score panel works correctly in the browser. Automated tests confirm structure and logic, but visual correctness (gauge fill direction, color coding, bar proportions, layout) requires human eyes.

Purpose: Gate before Phase 2 is considered complete. Catches visual defects that tests cannot catch.
Output: User confirmation that gauge, badge, factor bars, and scenario buttons all function correctly.
</objective>

<execution_context>
@/Users/chivonpowers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chivonpowers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-score-panel-redesign/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm green</name>
  <files></files>
  <action>
    Run the full test suite to confirm all tests pass before human verification:
      pytest tests/ -v

    If any tests fail, fix them before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>pytest tests/ -v 2>&1 | tail -10</automated>
  </verify>
  <done>pytest tests/ shows all tests PASSED with no failures or errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of score panel redesign</name>
  <what-built>
    Score panel redesign — Phase 2 complete:
    - SVG arc gauge displaying PD percentage with color coding (green/amber/red by tier)
    - Risk tier badge (Low / Moderate / High / Very High) inline below the gauge
    - Horizontal factor contribution bars (top 5, red rightward for risk-increasing, green leftward for risk-reducing)
    - Three scenario buttons (Prime Borrower, Borderline, High Risk) that pre-fill the features JSON
    - Raw JSON scoreView pre element removed
    - Phase 1 AI narrative still appearing below the panel
  </what-built>
  <how-to-verify>
    Start the server: uvicorn service.main:app --reload

    Visit: http://localhost:8000

    Test 1 — Score a HIGH RISK loan:
    1. Click "High Risk" scenario button
    2. Confirm the features textarea fills with JSON containing credit_score: 620, orig_ltv: 97, orig_dti: 49
    3. Confirm the textarea is editable (you can change values)
    4. Click "Score Loan"
    5. Expected: Arc gauge appears, filled RED to roughly 70-90% of the arc
    6. Expected: Risk tier badge shows "High" or "Very High" in red/pink
    7. Expected: Factor bars appear below the gauge — at least one bar extends right (red) for the dominant risk factor
    8. Expected: NO raw JSON pre element visible anywhere on the page
    9. Expected: AI narrative paragraph appears below the factor bars (may take a few seconds)

    Test 2 — Score a LOW RISK loan:
    1. Click "Prime Borrower" scenario button
    2. Click "Score Loan"
    3. Expected: Arc gauge appears, filled GREEN to roughly 5-20% of the arc
    4. Expected: Risk tier badge shows "Low" in green
    5. Expected: Factor bars appear (some may extend left in green for risk-reducing factors)

    Test 3 — Score a BORDERLINE loan:
    1. Click "Borderline" scenario button
    2. Click "Score Loan"
    3. Expected: Arc gauge filled AMBER/YELLOW to roughly 25-50%
    4. Expected: Risk tier badge shows "Moderate" in amber

    Test 4 — Visual check:
    - Gauge arc should fill left-to-right (low PD = small fill, high PD = large fill)
    - No raw JSON visible anywhere in the score section
    - Layout: gauge + badge group at top, factor bars below
    - Score narrative (AI text) appears below the factor bars when ANTHROPIC_API_KEY is set
  </how-to-verify>
  <resume-signal>Type "approved" if all visual checks pass, or describe any issues you see (e.g., "gauge fills backwards", "badge not showing", "factor bars missing")</resume-signal>
  <files></files>
  <action>Start server with uvicorn service.main:app --reload. Navigate to http://localhost:8000. Test all three scenarios per how-to-verify instructions. Confirm visual output matches expectations.</action>
  <verify>
    <automated>MISSING — visual verification is manual only</automated>
  </verify>
  <done>User types "approved" confirming gauge, badge, factor bars, and scenario buttons all work correctly.</done>
</task>

</tasks>

<verification>
Human confirms:
- Gauge fills correctly left-to-right with appropriate color for each tier
- Badge labels and colors match risk tier
- Factor bars visible and directional
- No raw JSON visible
- Scenario buttons work and textarea stays editable
- Full test suite green
</verification>

<success_criteria>
- pytest tests/ -v: all tests PASS
- User approves visual verification: gauge, badge, bars, scenario buttons all correct
- Phase 2 is complete and ready for Phase 3 planning
</success_criteria>

<output>
After completion, create .planning/phases/02-score-panel-redesign/02-03-SUMMARY.md
</output>
