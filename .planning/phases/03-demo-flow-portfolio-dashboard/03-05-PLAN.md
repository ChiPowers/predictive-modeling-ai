---
phase: 03-demo-flow-portfolio-dashboard
plan: 05
type: execute
wave: 4
depends_on:
  - 03-04
files_modified: []
autonomous: false
requirements:
  - DEMO-01
  - DEMO-02
  - DEMO-03
  - DEMO-04
  - PORT-01
  - PORT-02
  - PORT-03

must_haves:
  truths:
    - "Visitor clicks Run Full Demo and sees all five steps complete with checkmarks"
    - "Portfolio table shows sortable results after batch score"
    - "Donut chart segments are visually correct and color-coded by risk tier"
    - "AI insight paragraph appears below the donut chart"
  artifacts:
    - path: "service/static/index.html"
      provides: "Fully wired demo flow and portfolio section"
      contains: "runDemoBtn"
    - path: "service/static/app.js"
      provides: "runFullDemo, renderPortfolioTable, renderDonutChart"
      contains: "renderPortfolioTable"
  key_links:
    - from: "browser #runDemoBtn"
      to: "all 5 pipeline steps"
      via: "runFullDemo() orchestration"
      pattern: "runFullDemo"
---

<objective>
Human visual verification of the full Phase 3 implementation. Confirm the demo flow animates correctly, the portfolio table is sortable, the donut chart renders properly, and the AI insight loads.

Purpose: CSS animations, JS timing transitions, SVG visual rendering, and auto-scroll behavior cannot be verified by pytest. A human must confirm the experience is correct.

Output: Human approval or list of issues to fix.
</objective>

<execution_context>
@/Users/chivonpowers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chivonpowers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-demo-flow-portfolio-dashboard/03-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 3 implementation:
    - Run Full Demo button with 5-step animated checklist (seed → pipeline → activate → score → forecast)
    - Per-step spinner (active) and checkmark (done) animations
    - Auto-scroll to portfolio section after completion
    - Portfolio table: sortable 4-column table with risk tier badges
    - SVG donut chart: color-coded arc segments by risk tier
    - AI portfolio insight paragraph
  </what-built>
  <how-to-verify>
    Start the server: `uvicorn service.api:app --reload` (or however you normally run it).

    Open http://localhost:8000 in a browser.

    **Demo flow (DEMO-01 through DEMO-04):**
    1. Confirm the Run Demo card shows "Run Full Demo" button and no numbered list
    2. Click "Run Full Demo"
    3. Confirm step 1 (Seed demo data) shows a spinner while running, then fills with a checkmark on completion
    4. Confirm each subsequent step (Train pipeline, Activate model, Score loan, Run forecast) shows the same spinner → checkmark progression sequentially
    5. After all 5 steps complete, confirm the message "Your portfolio is ready. Here's what the model found." appears
    6. Confirm the page auto-scrolls to the Batch Score / portfolio section
    7. Confirm the button label changes to "Run Again"

    **Demo error handling (optional — skip if demo succeeds cleanly):**
    8. If any step fails: confirm that step shows a red ✕, remaining steps are greyed out, and a "Restart from beginning" button appears with a collapsible error detail

    **Portfolio table (PORT-01):**
    9. Submit the Batch Score form (use the pre-filled test data)
    10. Confirm the table appears with columns: Loan #, PD Score (%), Risk Tier, Top Risk Factor
    11. Confirm Risk Tier cells show color-coded badges (green/amber/red)
    12. Click "PD Score (%)" header — confirm rows re-sort (ascending/descending toggle)
    13. Click "Risk Tier" header — confirm rows re-sort alphabetically
    14. Confirm raw JSON is NOT visible (no pre element with batchView)

    **Donut chart (PORT-02):**
    15. Confirm a donut chart appears below the table with colored arc segments
    16. Confirm segment colors match tier colors: green (Low), amber (Moderate), orange (High), red (Very High)
    17. Confirm each segment shows a label with tier name and count

    **AI insight (PORT-03):**
    18. Confirm an AI-written paragraph appears below the donut chart
    19. Confirm the paragraph is specific to the portfolio (not generic)

    **Run Again:**
    20. Click "Run Again" — confirm all checkmarks reset to pending circles before the new run starts
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix (e.g., "donut chart blank", "sort breaks on second click", "step 3 hangs")</resume-signal>
</task>

</tasks>

<verification>
Human confirmation that all visual and interactive behaviors match the Phase 3 locked decisions from CONTEXT.md.
</verification>

<success_criteria>
- All 20 verification steps pass or issues are documented for targeted fixes
- Demo flow completes end-to-end with visible step animations
- Portfolio table sorts correctly on all four columns
- Donut chart renders with correct tier colors
- AI narrative appears with portfolio-specific content
</success_criteria>

<output>
After completion, create `.planning/phases/03-demo-flow-portfolio-dashboard/03-05-SUMMARY.md`
</output>
